"""
Tuple Methods

   Tuples have only two built-in methods:

   1) count()

      Returns the number of times a value occurs.

      tup = (10, 20, 10, 30, 10)
      tup.count(10)

      Output: 3


   2) index()

      Returns the index of the first occurrence of a value.

      tup = (10, 20, 30, 20)
      tup.index(20)

      Output: 1


============================================================
 Built-in Functions Used with Tuples

   1) len()
      Returns the number of elements.

   2) max()
      Returns the largest element.

   3) min()
      Returns the smallest element.

   4) sum()
      Returns the sum of numeric elements.

   5) sorted()
      Sorts the elements and returns a LIST.

   6) tuple()
      Converts another iterable into a tuple.

   Example:

   tup = (5, 10, 2, 8)

   len(tup)     -> 4
   max(tup)     -> 10
   min(tup)     -> 2
   sum(tup)     -> 25
   sorted(tup)  -> [2, 5, 8, 10]

"""